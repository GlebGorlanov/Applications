name: Build and Release Electron TicTacToe

on:
  push:
    branches: [ main ]
    tags:
      - 'v*.*.*'
    paths:
      - 'Tic-Tac-Toe Electron/**'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: Tic-Tac-Toe Electron/package-lock.json

      - name: Install dependencies
        working-directory: ./Tic-Tac-Toe Electron
        run: npm ci

      - name: Build for Windows
        working-directory: ./Tic-Tac-Toe Electron
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: TicTacToe-Windows
          path: Tic-Tac-Toe Electron/dist/*.exe
          if-no-files-found: warn

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Tic-Tac-Toe Electron/dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: Tic-Tac-Toe Electron/package-lock.json

      - name: Install dependencies
        working-directory: ./Tic-Tac-Toe Electron
        run: npm ci

      - name: Build for Linux
        working-directory: ./Tic-Tac-Toe Electron
        run: npm run build:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux AppImage
        uses: actions/upload-artifact@v4
        with:
          name: TicTacToe-Linux-AppImage
          path: Tic-Tac-Toe Electron/dist/*.AppImage
          if-no-files-found: warn

      - name: Upload Linux deb package
        uses: actions/upload-artifact@v4
        with:
          name: TicTacToe-Linux-deb
          path: Tic-Tac-Toe Electron/dist/*.deb
          if-no-files-found: warn

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Tic-Tac-Toe Electron/dist/*.AppImage
            Tic-Tac-Toe Electron/dist/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: Tic-Tac-Toe Electron/package-lock.json

      - name: Install dependencies
        working-directory: ./Tic-Tac-Toe Electron
        run: npm ci

      - name: Build for macOS
        working-directory: ./Tic-Tac-Toe Electron
        run: npm run build:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: TicTacToe-macOS
          path: Tic-Tac-Toe Electron/dist/*.dmg
          if-no-files-found: warn

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Tic-Tac-Toe Electron/dist/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}